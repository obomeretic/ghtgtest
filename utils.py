"""Utility functions for ghtgtest"""

def format_message(text: str) -> str:
    """Format a message for display"""
    return f"[INFO] {text}"

def calculate_sum(numbers: list) -> int:
    """Calculate sum of numbers"""
    return sum(numbers)
